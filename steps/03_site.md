# Step 3 — Site  ·  DONE (skeleton), grows with every session

**Goal** — a live GitHub Pages site students open on any device.

**Done**
- https://ebrahim-qareen.github.io/dgs-cybersec-diploma/ — home, curriculum (with SVG diagrams), sessions index, 18 session pages, labs, projects, tools, practice, 404
- One stylesheet `docs/assets/css/dgs.css`; all pages generated by `tools/build_site.py`; gate `testing/verify.py`
- Logo shown as the dark variant, no white plate; canvas fills the screen; sessions numbered 01–18

**Doing** — nothing.

**Next** — each built session replaces its placeholder page. Rule: edit the builder, run it, run the gate, commit, push from GitHub Desktop, check the live URL with a cache-busting `?v=` query.

**How to publish (every time)**
1. `python3 tools/build_site.py` → `python3 testing/verify.py` must say ALL CHECKS PASSED
2. commit → GitHub Desktop → Push origin
3. wait ~1 min, open the page with `?cb=1` to skip the browser cache
